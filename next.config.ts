/** @type {import('next').NextConfig} */
const nextConfig = {
  output: "export",
  distDir: "out",
  basePath: "/event-ticketing-platform/kamil",
  trailingSlash: true,
};

module.exports = nextConfig;